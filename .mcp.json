{
  "mcpServers": {
    "ssis-modernization-agent": {
      "type": "stdio",
      "command": "python",
      "args": [
        "-m",
        "ssis_modernization_agent.mcp_server"
      ],
      "cwd": "${workspaceFolder}",
      "env": {},
      "description": "SSIS → ADF conversion and deployment agent"
    }
  }
}